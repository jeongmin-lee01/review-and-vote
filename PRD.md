# PRD — 점메투

> **점메투** · 오늘의 메뉴에 투표하자
> 작성일: 2026-08-14 · 상태: 확정 v3.0
> 산출물: **소개 섹션 + 동작하는 데모 3화면**을 한 페이지에 결합한 단일 HTML
> 관련 문서: DESIGN.md

### v3.0 변경 이력
v2.0에서 "설명 없이 바로 동작"으로 전환했으나, 방향을 다시 정정한다. **이 페이지는 서비스를 소개하는 페이지가 맞다.** 다만 소개로 끝나지 않고, 소개 아래에 실제로 동작하는 데모가 이어진다.

> **구조**: 소개 스크롤 (문제 제기 → 솔루션 → 기능) → `체험해보기` 버튼 → 데모 3화면(S0 접속 → S1 탐색 → S2 투표 → S3 결과)

---

## 1. 서비스 한 문장 정의

> **약속 직전, 어디 갈지 못 정하는 20~30대 모임을 위해, 후보를 모아 친구들과 함께 투표로 한 곳을 정해주는 맛집 서비스.**

| 축 | 내용 |
| --- | --- |
| **누가** | 20~30대. 데이트·소규모 모임(2~6인) 장소를 정해야 하는 사람 |
| **언제** | 약속 직전. 오늘 저녁 어디 갈지 지금 정해야 하는 순간 |
| **왜** | 혼자 고르면 눈치 보이고, 단톡방에선 링크만 던지다 끝나기 때문 |

### 브랜드
- **서비스명**: 점메투 ('점메추'의 추천 → 투표)
- **서브카피**: 오늘의 메뉴에 투표하자
- **톤**: 20~30대 대상 가벼운 구어체. "오늘 여기서 먹자"가 관통 메시지

### 해결하려는 문제
- "아무거나 좋아" → 결정 지연 → 결국 늘 가던 곳
- 단톡방에 지도 링크 5개가 흩어져 비교 불가
- 리뷰는 많은데 다 읽을 시간이 없음
- 기존 지도·리뷰 앱은 **탐색**은 되지만 **합의**를 도와주지 않음

---

## 2. 페이지 구성 — 소개 + 데모

### A. 소개 섹션 (스크롤형)

| # | 섹션 | 담을 내용 |
| --- | --- | --- |
| S-1 | **Hero** | 큰 글씨 **점메투** + 서브카피 "오늘의 메뉴에 투표하자" + `체험해보기` 버튼 |
| S-2 | **문제 제기** | "아무거나 좋아"의 무한 루프 / 단톡방에 흩어진 링크 / 리뷰 읽을 시간 없음 — 공감 3컷 |
| S-3 | **솔루션 흐름** | 검색해서 후보 찾기 → AI 요약으로 빠르게 판단 → 투표로 확정. 3스텝 |
| S-4 | **핵심 기능** | AI 한 줄 요약 / 감성 도넛·워드클라우드 / 함께 투표. 카드 3개 |
| S-5 | **체험 유도** | "지금 바로 골라보자" + `체험해보기` 버튼 (아래 데모로 스크롤 또는 화면 전환) |

### B. 데모 섹션 (실제 동작, §4에서 상세 정의)

`체험해보기`를 누르면 소개 아래로 이어지거나, 별도 화면으로 전환된다 (구현 시 택1, §7 참고).

| # | 화면 | 요약 |
| --- | --- | --- |
| S0 | 접속 | 이름 입력, 재방문 시 기록 복원 |
| S1 | 후보 탐색 | 검색·필터 + AI 요약, 후보 담기 |
| S2 | 투표 | 셀렉트 그리드에서 실시간 투표 |
| S3 | 결과 | 확정 가게 + 도넛·워드클라우드 |

### 톤 & 무드
- 가볍고 짧은 구어체. "오늘 여기서 먹자"가 전체를 관통
- 음식 사진 전면 배치보다, **결정 과정**의 시각화가 차별점을 드러냄

---

## 3. 데모의 기능 범위

### 실제로 동작하는 것 ✅

| # | 기능 | 데모 구현 방식 |
| --- | --- | --- |
| F-1 | 리뷰 리스트 + 검색·필터 | 인라인 JSON 12곳. 키워드 검색 + 장르·가격대·지역 필터 실동작 |
| F-2 | AI 리뷰 요약 · 감성 분석 | **사전 생성한 요약·감성 수치를 JSON에 포함.** 실시간 호출 없음 |
| F-3 | 로그인 · 데이터 저장 | 이름 입력식 (when2meet 방식) + `localStorage` |
| F-4 | 대시보드 (도넛 · 워드클라우드) | 결과 화면에 CSS로 구현. 차트 라이브러리 미사용 |
| F-5 | 후보 담기 → 투표 → 확정 | 내 1표 + 가상 참여자 3명 자동 투표. 실시간 집계 |

### 이번에 하지 않는 것 ❌
- Gemini API 실시간 호출 (키 노출 위험)
- Supabase 실연동
- 실제 다인 실시간 투표 (백엔드 필요)
- 지도 API, 예약 연동

### 기술 제약 (반드시 지킬 것)
1. **데이터는 HTML 안에 `<script>`로 인라인한다.** 외부 `data.json`을 `fetch`하면 `file://`로 열 때 CORS에 막혀 화면이 빈다.
2. **`localStorage`는 브라우저 단위다.** 다른 기기·시크릿창에서 같은 이름을 입력하면 별개 계정이 된다. 화면에 이 사실을 명시한다.
3. 단일 HTML, 외부 JS 라이브러리 0개. 폰트 CDN만 사용.

---

## 4. 데이터 정책

- **실제 상호를 사용**하고, 화면 상단에 `샘플 데이터` 배지를 상시 노출한다.
- **부정 리뷰의 상한**: "웨이팅이 길다", "자리가 좁다", "주차가 어렵다" 등 **경미하고 사실 확인이 필요 없는 수준**까지만 작성한다. 맛·위생·서비스를 깎는 리뷰는 만들지 않는다. 실존 업체에 대한 허위 평판이 되기 때문이다.
- 감성 비율은 위 정책 안에서 산출한 값을 JSON에 고정 기재한다.
- 데이터 규모: 가게 12곳 × 리뷰 4~6개.
- **지역 구성**: 서울 6개 지역 × 2곳. 지역 필터가 실제로 목록을 갈라야 하므로 한 지역에 몰지 않는다. (예: 강남역·홍대·을지로·성수·연남·여의도)

### 가게 데이터 스키마
```
id, name, genre, price, area, aiSummary,
sentiment: { positive, neutral, negative },
keywords: [{ word, weight }],
reviews: [{ text, rating }]
```
> Phase 2에서 Supabase 테이블로 이관할 것을 전제로 설계한다.

---

## 5. 데모 화면 상세

소개(S-1~S-5)는 일반 스크롤이다. 데모(S0~S3)는 소개 아래에 이어지는 하나의 섹션 블록이며, 그 **안에서** S0→S1→S2→S3가 표시·숨김으로 전환된다. 페이지 전체를 막는 방식이 아니다.

### S0. 접속 (데모 진입 게이트)
- 스크롤이 데모 블록에 도달하거나 `체험해보기`로 이동하면 표시. **소개를 보는 동안은 뜨지 않는다**
- 이름 입력 → 그 이름의 프로필이 `localStorage`에 생성
- 재방문 시 같은 이름 입력하면 이전 기록 복원
- 비밀번호는 **선택 입력** (when2meet과 동일)
- 오락실 이니셜 입력 컨셉

### S1. 후보 탐색
- 가게 카드 리스트 (12곳)
- 키워드 검색 + 장르·가격대·지역 필터
- 각 카드에 **AI 한 줄 요약**을 리뷰 목록보다 위에 배치 (읽는 시간 단축이 목적)
- 카드에서 **후보 담기** — 최대 6곳
- 담긴 개수 표시 + "투표 시작" 버튼 (2곳 이상일 때 활성)

### S2. 투표
- 담은 후보가 **셀렉트 그리드** 칸에 배치됨
- 내가 한 칸 선택 → 즉시 1표
- 이후 가상 참여자 **2P·3P·4P가 시간차를 두고 자동 투표** (약 0.8초 간격)
- 각 표가 들어올 때마다 점수판 갱신
- 총 4표 집계 완료 시 결과로 전환

### S3. 결과
- 확정된 가게 1곳 크게 표시
- **도넛 차트**: 해당 가게 리뷰의 긍정/중립/부정 비율
- **워드클라우드**: 리뷰 키워드를 빈도에 따라 다른 크기로
- 득표 분포 (누가 어디에 투표했는지)
- "다시 투표" 버튼 → S1으로 복귀
- 내 투표 기록은 `localStorage`에 저장, 재방문 시 지난 결과 확인 가능

### 판정 규칙
| 상황 | 처리 |
| --- | --- |
| 총 표수 | 4표 (나 1 + 가상 참여자 3) |
| **동점** | **내가 선택한 쪽이 승리.** 내 표가 장식이 되지 않게 하는 규칙 |
| 3자 동점 | 내 선택 우선 적용 후, 남은 동점은 담은 순서가 빠른 쪽 |
| 재투표 | 가상 참여자의 선택은 매번 새로 뽑는다 |

---

## 6. 로드맵

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| **Phase 1 — 소개 + 데모** | 위 S-1~S-5(소개) + S0~S3(데모) 전체. 단일 HTML | ⬅️ **여기** |
| **Phase 2 — 실연동** | Gemini 실호출(서버 경유), Supabase Auth·DB 이관, 실데이터 확보 | |
| **Phase 3 — 실시간 다인 투표** | 초대 링크, 여러 기기에서 동시 참여 | 서비스명이 완성되는 지점 |
| **Phase 4 — 콘텐츠** | 자체 리뷰 작성·별점 | |
| **Phase 5 — 확장** | 분위기 큐레이션, 예약 연동, 추천 알고리즘 | |

---

## 7. 다음 작업 — 구현 착수

### 확정된 사항
| 항목 | 결정 |
| --- | --- |
| 소개 → 데모 전환 방식 | **스크롤 이어붙이기.** `체험해보기` 클릭 시 데모 시작 지점(S0)까지 부드럽게 스크롤. 화면 전환(숨김/표시) 아님 |

### 구현 순서
1. 소개 섹션 S-1~S-5 뼈대 (기존 v1 자산 재활용 가능)
2. 데모 데이터 12곳 작성 (§4 스키마)
3. S0 접속 화면
4. S1 탐색 (검색·필터·AI 요약·후보 담기)
5. S2 투표 (셀렉트 그리드, 자동 투표)
6. S3 결과 (도넛·워드클라우드)
7. 소개-데모 전환 연결 + 반응형 + 스타필드 배경

---

## 8. 완료 기준

- [ ] 소개 섹션 S-1~S-5가 스크롤로 자연스럽게 이해된다
- [ ] `체험해보기` 버튼 클릭 시 데모(S0) 지점까지 부드럽게 스크롤된다
- [ ] 이름 입력 후 재방문 시 기록이 복원된다
- [ ] 검색·필터가 실제로 목록을 걸러낸다
- [ ] 후보를 담고 투표해 결과까지 도달한다
- [ ] 가상 참여자 3명의 표가 시간차로 들어온다
- [ ] 동점 시 내 선택이 이긴다
- [ ] 도넛·워드클라우드가 선택된 가게 데이터를 반영한다
- [ ] 모바일 360px에서 전 화면 정상 동작
- [ ] `샘플 데이터` 배지 및 `localStorage` 안내 노출
- [ ] `file://`로 열어도 동작 (인라인 데이터 확인)
- [ ] 외부 JS 라이브러리 0개

---

## 9. 확정 · 미결정 사항

### 확정 (변경 가능)
- **가상 참여자 이름**: `2P` / `3P` / `4P`. 사람 이름을 지어내면 정체가 모호해지므로, 가상 참여자임을 숨기지 않는 표기를 쓴다.
- **워드클라우드 키워드**: **수동 지정.** 한국어는 조사·어미 변형 탓에 자동 추출에 형태소 분석기가 필요한데, 이는 "라이브러리 0개" 제약과 충돌한다. 데이터 작성 시 가게별로 키워드 5~6개를 `weight`(1~5)와 함께 직접 기입한다.

### 미결정
- (없음) — 구현 착수 가능
