# DESIGN — 점메투 랜딩페이지

> 작성일: 2026-08-14 · 상태: 확정 v3.0
> 대상: PRD.md §2 소개 섹션(S-1~S-5) + §5 데모 화면(S0~S3)
> 관련 문서: PRD.md

---

## 1. 무드

### 한 줄 정의
> **90년대 오락실 게임의 "캐릭터 셀렉트" 화면을, 맛집 고르는 순간으로 옮긴 CRT 화면.**

### 왜 이 방향인가
'점메투'의 핵심 동작은 **여러 후보 중 하나를 고르는 것**이다. 오락실 캐릭터 셀렉트 화면은 정확히 같은 구조를 가진다. 칸에 후보가 놓이고, 커서가 움직이고, 하나를 확정한다. 따라서 레트로는 장식이 아니라 **서비스 동작의 은유**로 기능한다.

### 강도: 하이브리드
| 요소 | 처리 |
| --- | --- |
| 제목 · 버튼 · 라벨 · 숫자 | 도트폰트, 각진 모서리, 하드 섀도우 |
| 본문 | 읽히는 고딕 (Pretendard) |
| CRT 질감 (스캔라인·빛번짐) | **히어로 섹션에만** 집중 |

> **원칙**: 힘은 한 군데서만 준다. 전 섹션에 스캔라인을 깔면 레트로가 아니라 피로가 된다.

### 피할 것
- 모든 섹션에 애니메이션 넣기 → 산만하고 "AI가 만든 티"가 난다
- 이모지로 레트로 흉내내기 (🕹️👾) → 도트 그래픽의 격을 떨어뜨린다
- 둥근 모서리 (`border-radius`) → 픽셀 감성과 정면 충돌. **기본값 0**

---

## 2. 컬러

### 콘셉트
90년대 CRT 모니터에는 **그린 인광(P1)** 과 **앰버 인광(P3)** 두 종류가 있었다. 이 둘만으로 팔레트를 구성한다. 그린은 기본, 앰버는 강조 — 즉 **"앰버가 켜진 곳이 눌러야 할 곳"** 이라는 규칙이 페이지 전체를 관통한다.

### 토큰

| 토큰 | HEX | 용도 |
| --- | --- | --- |
| `--bg` | `#0A0E0A` | 페이지 배경. 순수 검정 대신 미세한 녹빛 |
| `--panel` | `#111A12` | 카드·그리드 칸 배경 |
| `--green` | `#3DFF6E` | 기본 텍스트, 테두리, 로고 |
| `--green-dim` | `#1F7A3A` | 비활성 상태, 구분선, 보조 그래픽 |
| `--amber` | `#FFB000` | **CTA 버튼, 선택된 칸, 득표수, 강조 단어** |
| `--ink` | `#DFFFE4` | 본문 장문 텍스트 (그린 틴트 화이트) |
| `--star` | `#FFFFFF` | **배경 별 전용 예외색.** 이 용도 외에는 쓰지 않는다 (§9) |

### 사용 규칙
1. **앰버는 페이지당 5곳 이하.** 남발하면 강조가 사라진다.
2. **본문 장문에 `--green`을 쓰지 않는다.** 형광 그린으로 긴 글을 읽으면 눈이 아프다. 장문은 `--ink`, 짧은 라벨·제목만 `--green`.
3. `--green-dim`은 텍스트로 쓰지 않는다. 배경 대비가 부족하다. 선과 비활성 요소 전용.
4. 음식 사진을 넣는다면 그린 오버레이를 씌우지 않는다. 음식만은 원래 색으로.

### 접근성
- `--ink` / `--green` / `--amber` 모두 `--bg` 대비 명도차가 충분하다
- 포커스 표시는 `outline: 2px solid var(--amber)` 로 통일. 키보드 사용자가 커서를 잃지 않게 한다

---

## 3. 타이포

### 폰트

| 역할 | 폰트 | 비고 |
| --- | --- | --- |
| 디스플레이 (히어로) | **Galmuri14** | 큰 크기에서 도트가 또렷함 |
| 제목 · 버튼 · 라벨 · 숫자 | **Galmuri11** | 점수판·UI 텍스트 전용 |
| 본문 | **Pretendard** | 가독성 담당 |

**불러오기** — CSS 최상단 2줄:
```css
@import url('https://cdn.jsdelivr.net/npm/galmuri/dist/galmuri.css');
@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css');
```

**라이선스**: Galmuri는 SIL Open Font License 1.1 (제작: 이민서). 폰트 자체를 판매하지 않는 한 상업적 사용 가능. Pretendard 역시 OFL. 포트폴리오·상업 이용 모두 문제없음.

### ⚠️ 도트폰트 필수 규칙
비트맵 폰트는 **설계 크기의 정수배**에서만 선명하다. 어긋나면 글자가 뿌옇게 뭉개진다.

- Galmuri11 → **11 / 22 / 33 / 44px**
- Galmuri14 → **14 / 28 / 42 / 56 / 70px**
- 도트폰트에 `clamp()`나 `vw` 단위를 쓰지 않는다. 반응형은 미디어쿼리로 **정수배 단계를 바꾼다.**
- 도트폰트 요소에 `-webkit-font-smoothing: none;` 을 넣으면 더 또렷하다.

### 타입 스케일

| 용도 | 폰트 | 크기 | 비고 |
| --- | --- | --- | --- |
| 히어로 로고 "점메투" | Galmuri14 | 70px (모바일 42px) | letter-spacing 0.05em |
| 서브카피 "오늘의 메뉴를 검색하고, 다함께 투표하자" | Galmuri11 | 22px (모바일 11px) | 로고 바로 아래, 작게 |
| 섹션 제목 | Galmuri11 | 33px (모바일 22px) | |
| 본문 | Pretendard | 16px / 행간 1.7 | 400 |
| 라벨 · 배지 | Galmuri11 | 11px | 영문은 대문자, letter-spacing 0.1em |
| 숫자 (득표수) | Galmuri11 | 33px | `--amber` |

---

## 4. 레이아웃

- **그리드**: 8px 배수로 모든 간격 통일 (픽셀 감성과 맞음)
- **컨테이너**: 최대 1080px, 좌우 여백 24px
- **섹션 간격**: 데스크톱 96px / 모바일 64px
- **모서리**: `border-radius: 0` 전역
- **테두리**: `2px solid` — 1px는 도트 느낌이 안 산다
- **그림자**: 블러 없는 하드 섀도우만. `box-shadow: 4px 4px 0 var(--green-dim)`

---

## 5. 시그니처 — 캐릭터 셀렉트 그리드

**장식이 아니라 실제 투표 화면(S2)이다.** 담은 후보가 칸에 들어가고, 그 칸을 눌러 진짜 투표한다.

```
┌──────────────────────────────────────┐
│  점메투            1P: MIN  [샘플]    │  ← 상단 바 (로고 작게 + 접속자)
│                                      │
│         SELECT YOUR DINNER           │  ← Galmuri11 라벨
│                                      │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐         │
│  │국밥│ │마라│ │파스│ │초밥│          │  ← 담은 후보 칸 (2px 그린)
│  └─3──┘ └████┘ └─1──┘ └─0──┘         │  ← 하단 숫자 = 득표수(앰버)
│                                      │
│   1P ●  2P ●  3P ●  4P ○             │  ← 투표 도착 표시기
│                                      │
│   ▸ 오늘 여기서 먹자                   │  ← 앰버 CTA
└──────────────────────────────────────┘
   ░░░ 스캔라인 오버레이 (이 화면에만) ░░░
```

### 동작
1. 내가 칸을 선택하면 즉시 1표가 꽂히고, 그 칸이 앰버로 점멸
2. 이어서 2P·3P·4P의 표가 약 0.8초 간격으로 도착하며 각 칸 득표수가 오른다
3. 표가 도착할 때마다 하단 표시기가 `○ → ●`로 채워진다
4. 4표가 모이면 결과 화면으로 전환

> **의도**: 방문자는 클릭 한 번으로 "여러 명이 투표해 하나가 정해지는" 서비스의 핵심을 직접 겪는다. 설명 문장이 필요 없다.

---

## 6. CSS 애니메이션 목록

난이도는 비전공자 기준. **★☆☆부터 만들고, 시간이 남으면 ★★☆로 올린다.**

| # | 효과 | 구현 | 난이도 |
| --- | --- | --- | --- |
| A-1 | 커서·테두리 점멸 | `@keyframes` + `animation-timing-function: steps(1)` | ★☆☆ |
| A-2 | 버튼 눌림 | `:active` 시 `translate(2px,2px)` + 그림자 축소 | ★☆☆ |
| A-3 | 스캔라인 | `repeating-linear-gradient` + 느린 `translateY`, `pointer-events: none` | ★☆☆ |
| A-4 | 표 도착 시 득표수 갱신 | 값 변경 시 `transform: scale()` 짧게 | ★★☆ |
| A-5 | 타이핑 효과 | `steps()` + `width` 애니메이션 | ★★☆ |
| A-6 | 득표수 카운트업 | CSS `@property` — 브라우저 지원 편차 있음 | ★★★ |

### 필수 처리
```css
@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; transition: none !important; }
}
```
멀미·발작 민감도가 있는 사용자를 위한 표준 대응이며, 점멸이 많은 이 디자인에서는 특히 중요하다.

### 절제 기준
애니메이션은 **투표 화면(S2)과 버튼(A-2)** 에 몰아준다. 기능이 많은 S1 탐색 화면과 정보를 읽어야 하는 S3 결과 화면은 정적으로 두는 편이 오히려 S2를 돋보이게 한다.

---

## 7. 소개 섹션 디자인 지침 (S-1 ~ S-5)

PRD §2-A 소개 섹션에 대응한다. 데모(§8)보다 **애니메이션을 절제**한다 — 스크롤로 읽는 구간이므로 움직임이 많으면 오히려 읽기를 방해한다.

| 섹션 | 디자인 방향 |
| --- | --- |
| **S-1 Hero** | §6 시그니처와는 별개로, 여기선 정적인 히어로. 큰 글씨 **점메투**(Galmuri14) + 서브카피 + `체험해보기` 버튼(앰버). 스타필드 배경(§10)만 상시 흐름 |
| **S-2 문제 제기** | 3개 문제를 도트 라벨 + 짧은 문장으로. 배경은 `--panel`, 애니메이션 없음 |
| **S-3 솔루션 흐름** | 3스텝 가로 배치. 각 스텝에 `STEP 01/02/03` 라벨 |
| **S-4 핵심 기능** | 카드 3개. 2px 테두리 + 하드 섀도우. 아이콘은 이모지 대신 도트풍 도형이나 텍스트 기호 |
| **S-5 체험 유도** | 짧은 한 줄 카피 + `체험해보기` 버튼(앰버, S-1과 같은 스타일로 재등장) |

### 소개 → 데모 전환
`체험해보기` 버튼 클릭 시 **부드러운 스크롤**(`scrollIntoView({behavior:'smooth'})`)로 데모 블록(S0) 지점까지 이동한다. 화면이 사라지고 바뀌는 방식이 아니라 한 페이지 안의 이동이다. 전환 지점에 시각적 경계(구분선 또는 배경색 전환)를 둬서 "여기부터 실제로 써보는 구간"임을 알린다.

---

## 8. 데모 화면 디자인 지침 (S0 ~ S3)

PRD §5의 S0 ~ S3에 대응한다.

| 화면 | 디자인 방향 |
| --- | --- |
| **S0 접속** | 데모 블록 시작 지점에 오락실 이니셜 입력창. 페이지 전체를 덮지 않고 **데모 블록 영역에 한정**된다. `ENTER YOUR NAME` 라벨 + 커서 점멸(A-1). 비밀번호는 `(선택)` 표기로 작게. 아래에 `이 브라우저에만 저장됩니다` 안내 |
| **S1 후보 탐색** | 기능이 많은 화면이므로 **가장 조용하게.** 애니메이션 없음. 검색창·필터칩은 2px 테두리 사각형. 카드는 `--panel` 배경 + 하드 섀도우. AI 요약을 카드 상단에 앰버 라벨 `AI 요약`과 함께, 리뷰 목록은 접어둠. 담기 버튼은 담긴 상태에서 앰버로 반전 |
| **S2 투표** | §5 시그니처 그대로. **스캔라인은 이 화면에만.** 이 데모의 유일한 "쇼" |
| **S3 결과** | 확정 가게명을 Galmuri14로 크게 + 앰버. 도넛은 `conic-gradient`로 구현 (긍정=그린, 중립=그린dim, 부정=앰버). 워드클라우드는 키워드마다 폰트 크기만 차등, 회전 없이 가로쓰기 |
| **공통 상단바** | 로고 작게 + 접속자 이름 + `샘플 데이터` 배지(앰버 테두리, 채움 없음). 전 화면 고정 |

### 화면 전환
`display: none` 토글로 충분하다. 전환 애니메이션은 넣더라도 짧은 페이드 한 종류로 통일한다. 화면마다 다른 전환 효과를 주면 산만해진다.

---

## 9. 카피 톤

- 짧은 구어체. "오늘 여기서 먹자"가 관통 메시지
- 게임 문법을 빌리되 남용하지 않는다: `PRESS START`, `SELECT`, `1P / 2P` 정도까지
- 버튼은 결과를 그대로 말한다. "제출" ❌ → "오늘 여기서 먹자" ✅
- 아직 없는 기능은 숨기지 않고 `예정` 배지로 표기 (PRD §3)

---

## 10. 배경 — 스타필드

### 정의
접속 중 상시 재생되는 **패럴랙스 스타필드**. 흰 도트가 아래에서 위로 천천히 흐른다. 90년대 종스크롤 슈팅게임(갤러그·1943)의 배경 문법을 그대로 가져온 것으로, 위 방향은 "전진하는" 감각을 만든다.

### 규칙
| 항목 | 값 |
| --- | --- |
| 배경색 | `--bg` 유지. 별만 `--star`(흰색) |
| 별 투명도 | 레이어별 `0.25 / 0.45 / 0.7` — 원근감 |
| 별 크기 | 1px 다수 + 2px 소수 |
| 이동 속도 | 한 바퀴 40~60초. 눈에 띄면 실패 |
| 레이어 | 2~3장. 각기 다른 속도 |
| 위치 | `position: fixed; inset: 0; z-index: -1;` |

### 구현
`radial-gradient` 레이어 + `background-position` 애니메이션. **JS·라이브러리 불필요.**

```css
.starfield {
  position: fixed; inset: 0; z-index: -1; pointer-events: none;
  background-image:
    radial-gradient(1px 1px at 20% 30%, var(--star) 100%, transparent 0),
    radial-gradient(1px 1px at 70% 80%, var(--star) 100%, transparent 0),
    radial-gradient(2px 2px at 45% 15%, var(--star) 100%, transparent 0);
  background-size: 200px 200px, 300px 300px, 250px 250px;
  opacity: 0.45;
  animation: drift 50s linear infinite;
}
@keyframes drift {
  to { background-position: 0 -200px, 0 -300px, 0 -250px; }
}
```

> **이음새 없는 반복**: 이동 거리를 `background-size`와 정확히 같은 값(또는 정수배)으로 맞춘다. 어긋나면 반복 지점에서 별이 튄다.

### 주의
1. **그라디언트 정지점을 `100%`에서 끊는다.** 부드럽게 번지면 도트가 아니라 흐릿한 점이 되어 픽셀 감성과 어긋난다.
2. **트윙클(반짝임)은 넣지 않는다.** 스캔라인을 S2에만 몰아주기로 정했는데(§6) 배경까지 상시 점멸하면 눈이 쉴 곳이 없다.
3. **흰색은 이 용도 한정이다.** 별이 CTA 앰버보다 튀면 안 되므로 `opacity`로 확실히 눌러둔다.
4. `prefers-reduced-motion`에서 흐름을 멈추고 정지된 별만 남긴다.
5. 별을 S2 셀렉트 그리드 뒤에 두더라도 `--panel` 배경으로 칸이 덮이므로 가독성 문제는 없다.

---

## 11. 품질 체크리스트

- [ ] 소개 섹션(S-1~S-5)이 데모보다 정적인가 — 애니메이션 과다 없음
- [ ] `체험해보기` 버튼 클릭 시 데모(S0) 지점까지 부드럽게 스크롤되는가
- [ ] 모바일 360px에서 셀렉트 그리드가 2열로 재배치되는가
- [ ] 도트폰트 크기가 전부 정수배인가 (뭉갠 글자 없는지 확대해서 확인)
- [ ] 앰버가 화면당 5곳 이하인가
- [ ] 키보드 Tab으로 모든 버튼에 도달하고 포커스가 보이는가
- [ ] `prefers-reduced-motion` 적용되는가
- [ ] 본문 장문에 형광 그린을 쓰지 않았는가
- [ ] `border-radius`가 0인가
- [ ] `샘플 데이터` 배지가 전 화면에 보이는가
- [ ] 스캔라인이 S2에만 있는가
- [ ] 스타필드 반복 지점에서 별이 튀지 않는가
- [ ] 흰색이 배경 별 외에 쓰이지 않았는가
