# DESIGN — 점메투 랜딩페이지

> 작성일: 2026-08-14 · 상태: 확정 v1.0
> 대상: PRD.md §5 랜딩페이지 (단일 HTML, CSS 애니메이션)
> 관련 문서: PRD.md

---

## 1. 무드

### 한 줄 정의
> **90년대 오락실 게임의 "캐릭터 셀렉트" 화면을, 맛집 고르는 순간으로 옮긴 CRT 화면.**

### 왜 이 방향인가
'점메투'의 핵심 동작은 **여러 후보 중 하나를 고르는 것**이다. 오락실 캐릭터 셀렉트 화면은 정확히 같은 구조를 가진다. 칸에 후보가 놓이고, 커서가 움직이고, 하나를 확정한다. 따라서 레트로는 장식이 아니라 **서비스 동작의 은유**로 기능한다.

### 강도: 하이브리드
| 요소 | 처리 |
| --- | --- |
| 제목 · 버튼 · 라벨 · 숫자 | 도트폰트, 각진 모서리, 하드 섀도우 |
| 본문 | 읽히는 고딕 (Pretendard) |
| CRT 질감 (스캔라인·빛번짐) | **히어로 섹션에만** 집중 |

> **원칙**: 힘은 한 군데서만 준다. 전 섹션에 스캔라인을 깔면 레트로가 아니라 피로가 된다.

### 피할 것
- 모든 섹션에 애니메이션 넣기 → 산만하고 "AI가 만든 티"가 난다
- 이모지로 레트로 흉내내기 (🕹️👾) → 도트 그래픽의 격을 떨어뜨린다
- 둥근 모서리 (`border-radius`) → 픽셀 감성과 정면 충돌. **기본값 0**

---

## 2. 컬러

### 콘셉트
90년대 CRT 모니터에는 **그린 인광(P1)** 과 **앰버 인광(P3)** 두 종류가 있었다. 이 둘만으로 팔레트를 구성한다. 그린은 기본, 앰버는 강조 — 즉 **"앰버가 켜진 곳이 눌러야 할 곳"** 이라는 규칙이 페이지 전체를 관통한다.

### 토큰

| 토큰 | HEX | 용도 |
| --- | --- | --- |
| `--bg` | `#0A0E0A` | 페이지 배경. 순수 검정 대신 미세한 녹빛 |
| `--panel` | `#111A12` | 카드·그리드 칸 배경 |
| `--green` | `#3DFF6E` | 기본 텍스트, 테두리, 로고 |
| `--green-dim` | `#1F7A3A` | 비활성 상태, 구분선, 보조 그래픽 |
| `--amber` | `#FFB000` | **CTA 버튼, 선택된 칸, 득표수, 강조 단어** |
| `--ink` | `#DFFFE4` | 본문 장문 텍스트 (그린 틴트 화이트) |

### 사용 규칙
1. **앰버는 페이지당 5곳 이하.** 남발하면 강조가 사라진다.
2. **본문 장문에 `--green`을 쓰지 않는다.** 형광 그린으로 긴 글을 읽으면 눈이 아프다. 장문은 `--ink`, 짧은 라벨·제목만 `--green`.
3. `--green-dim`은 텍스트로 쓰지 않는다. 배경 대비가 부족하다. 선과 비활성 요소 전용.
4. 음식 사진을 넣는다면 그린 오버레이를 씌우지 않는다. 음식만은 원래 색으로.

### 접근성
- `--ink` / `--green` / `--amber` 모두 `--bg` 대비 명도차가 충분하다
- 포커스 표시는 `outline: 2px solid var(--amber)` 로 통일. 키보드 사용자가 커서를 잃지 않게 한다

---

## 3. 타이포

### 폰트

| 역할 | 폰트 | 비고 |
| --- | --- | --- |
| 디스플레이 (히어로) | **Galmuri14** | 큰 크기에서 도트가 또렷함 |
| 제목 · 버튼 · 라벨 · 숫자 | **Galmuri11** | 점수판·UI 텍스트 전용 |
| 본문 | **Pretendard** | 가독성 담당 |

**불러오기** — CSS 최상단 2줄:
```css
@import url('https://cdn.jsdelivr.net/npm/galmuri/dist/galmuri.css');
@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css');
```

**라이선스**: Galmuri는 SIL Open Font License 1.1 (제작: 이민서). 폰트 자체를 판매하지 않는 한 상업적 사용 가능. Pretendard 역시 OFL. 포트폴리오·상업 이용 모두 문제없음.

### ⚠️ 도트폰트 필수 규칙
비트맵 폰트는 **설계 크기의 정수배**에서만 선명하다. 어긋나면 글자가 뿌옇게 뭉개진다.

- Galmuri11 → **11 / 22 / 33 / 44px**
- Galmuri14 → **14 / 28 / 42 / 56 / 70px**
- 도트폰트에 `clamp()`나 `vw` 단위를 쓰지 않는다. 반응형은 미디어쿼리로 **정수배 단계를 바꾼다.**
- 도트폰트 요소에 `-webkit-font-smoothing: none;` 을 넣으면 더 또렷하다.

### 타입 스케일

| 용도 | 폰트 | 크기 | 비고 |
| --- | --- | --- | --- |
| 히어로 로고 "점메투" | Galmuri14 | 70px (모바일 42px) | letter-spacing 0.05em |
| 서브카피 "오늘의 메뉴에 투표하자" | Galmuri11 | 22px (모바일 11px) | 로고 바로 아래, 작게 |
| 섹션 제목 | Galmuri11 | 33px (모바일 22px) | |
| 본문 | Pretendard | 16px / 행간 1.7 | 400 |
| 라벨 · 배지 | Galmuri11 | 11px | 영문은 대문자, letter-spacing 0.1em |
| 숫자 (득표수) | Galmuri11 | 33px | `--amber` |

---

## 4. 레이아웃

- **그리드**: 8px 배수로 모든 간격 통일 (픽셀 감성과 맞음)
- **컨테이너**: 최대 1080px, 좌우 여백 24px
- **섹션 간격**: 데스크톱 96px / 모바일 64px
- **모서리**: `border-radius: 0` 전역
- **테두리**: `2px solid` — 1px는 도트 느낌이 안 산다
- **그림자**: 블러 없는 하드 섀도우만. `box-shadow: 4px 4px 0 var(--green-dim)`

---

## 5. 시그니처 — 캐릭터 셀렉트 그리드

**히어로에 들어가는 이 페이지의 유일한 "쇼"다.** 나머지는 전부 조용하게 간다.

```
┌──────────────────────────────────────┐
│         점 메 투                      │  ← Galmuri14, 그린
│   오늘의 메뉴에 투표하자                │  ← Galmuri11, 작게
│                                      │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐         │
│  │국밥│ │마라│ │파스│ │초밥│          │  ← 후보 칸 (2px 그린 테두리)
│  └────┘ └████┘ └────┘ └────┘         │  ← 선택 칸: 앰버 테두리 + 점멸
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐         │
│  │삼겹│ │쌀국│ │돈까│ │족발│          │
│  └────┘ └────┘ └────┘ └────┘         │
│                                      │
│   ▸ 오늘 여기서 먹자                   │  ← 앰버 CTA 버튼
└──────────────────────────────────────┘
   ░░░ 스캔라인 오버레이 (히어로에만) ░░░
```

### 동작
1. 페이지 로드 시 커서(앰버 테두리)가 칸을 **자동으로 순회**한다 — 오락실 데모 모드
2. 마우스를 올리거나 키보드로 포커스하면 그 칸이 선택된다
3. 선택된 칸의 테두리가 점멸하고, 칸 이름이 CTA 버튼 문구에 반영되면 더 좋다

> **의도**: 방문자는 첫 화면에서 "여러 후보 중 하나를 고른다"는 서비스의 동작을 설명 없이 이해한다.

---

## 6. CSS 애니메이션 목록

난이도는 비전공자 기준. **★☆☆부터 만들고, 시간이 남으면 ★★☆로 올린다.**

| # | 효과 | 구현 | 난이도 |
| --- | --- | --- | --- |
| A-1 | 커서·테두리 점멸 | `@keyframes` + `animation-timing-function: steps(1)` | ★☆☆ |
| A-2 | 버튼 눌림 | `:active` 시 `translate(2px,2px)` + 그림자 축소 | ★☆☆ |
| A-3 | 스캔라인 | `repeating-linear-gradient` + 느린 `translateY`, `pointer-events: none` | ★☆☆ |
| A-4 | 셀렉트 그리드 자동 순회 | 칸마다 `animation-delay`를 순차 지정 | ★★☆ |
| A-5 | 타이핑 효과 | `steps()` + `width` 애니메이션 | ★★☆ |
| A-6 | 득표수 카운트업 | CSS `@property` — 브라우저 지원 편차 있음 | ★★★ |

### 필수 처리
```css
@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; transition: none !important; }
}
```
멀미·발작 민감도가 있는 사용자를 위한 표준 대응이며, 점멸이 많은 이 디자인에서는 특히 중요하다.

### 절제 기준
애니메이션은 **히어로(A-1·A-3·A-4)와 버튼(A-2)** 에 몰아준다. S-2 이후 본문 섹션은 정적으로 두는 편이 오히려 히어로를 돋보이게 한다.

---

## 7. 섹션별 디자인 지침

PRD §5의 S-1 ~ S-7에 대응한다.

| 섹션 | 디자인 방향 |
| --- | --- |
| **S-1 Hero** | §5 시그니처 그대로. 스캔라인 여기에만 |
| **S-2 문제 제기** | "GAME OVER" 톤. 3개 문제를 도트 라벨 + 짧은 문장으로. 배경은 `--panel`, 애니메이션 없음 |
| **S-3 솔루션 흐름** | 3스텝 가로 배치. 각 스텝에 `STEP 01/02/03` 라벨(실제 순서이므로 번호가 정당함). 3번째 스텝에 앰버 `예정` 배지 |
| **S-4 핵심 기능** | 카드 3개. 2px 테두리 + 하드 섀도우. 아이콘은 이모지 대신 도트풍 도형 또는 텍스트 기호 |
| **S-5 사용 시나리오** | 대화창 형식. "금요일 저녁 7시, 강남역 4명" — RPG 대사창처럼 테두리 박스 + 아래쪽 삼각형 커서 |
| **S-6 최종 CTA** | 앰버 버튼 크게. 제출 후 "등록 완료" 상태로 전환 (백엔드 없음) |
| **S-7 Footer** | Galmuri11 11px, `--green-dim`. 제작자·깃허브 링크 |

---

## 8. 카피 톤

- 짧은 구어체. "오늘 여기서 먹자"가 관통 메시지
- 게임 문법을 빌리되 남용하지 않는다: `PRESS START`, `SELECT`, `1P / 2P` 정도까지
- 버튼은 결과를 그대로 말한다. "제출" ❌ → "오늘 여기서 먹자" ✅
- 아직 없는 기능은 숨기지 않고 `예정` 배지로 표기 (PRD §3)

---

## 9. 품질 체크리스트

- [ ] 모바일 360px에서 셀렉트 그리드가 4×2 → 2×4로 재배치되는가
- [ ] 도트폰트 크기가 전부 정수배인가 (뭉갠 글자 없는지 확대해서 확인)
- [ ] 앰버가 5곳 이하인가
- [ ] 키보드 Tab으로 모든 버튼에 도달하고 포커스가 보이는가
- [ ] `prefers-reduced-motion` 적용되는가
- [ ] 본문 장문에 형광 그린을 쓰지 않았는가
- [ ] `border-radius`가 0인가
