# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

점메투 ("점메추" 말장난 → 투표) — 포트폴리오 프로젝트. 소규모 모임(2~6인)이 단톡방에서 끝없이 다투는 대신 투표로 맛집을 정하도록 돕는 서비스. 브랜드 톤은 짧고 가벼운 구어체 ("오늘 여기서 먹자"가 전체를 관통하는 메시지).

현재 범위는 **Phase 1(랜딩페이지)뿐**이다. 이 저장소에는 아직 백엔드, 데이터 레이어, 빌드 도구가 없다 — 어떤 기능이 실제로 존재하는지 가정하기 전에 아래 "로드맵" 섹션을 먼저 확인할 것.

## 파일 구성

- `index.html` — 랜딩페이지 전체. 인라인 `<style>` + 인라인 `<script>`로 구성된 단일 HTML 파일이며, 빌드 단계도 설치할 의존성도 없다.
- `PRD.md` — 제품 스펙: 문제 정의, Phase별 기능 목록, 랜딩페이지 섹션 구성(S-1~S-7), 카피 방향.
- `DESIGN.md` — 랜딩페이지의 확정(v1.0) 비주얼 디자인 시스템. `index.html`의 CSS를 수정하기 전에 반드시 먼저 읽을 것.

## 실행 / 미리보기

빌드 단계 없음. 다음 중 하나로 확인한다.
- `index.html`을 브라우저로 직접 열거나,
- 로컬 서버로 띄운다 (자동화된 브라우저 도구로 테스트할 때는 `file://`이 차단되는 경우가 있어 필요): `npx --yes serve -l 8811 .` 실행 후 `http://localhost:8811/index.html` 접속.
- 주의: 이 머신의 `python`은 Windows Store 스텁으로 연결되어 실제 인터프리터가 아니다 — `python -m http.server` 대신 `npx serve`를 쓸 것.

이 저장소에는 테스트, 린터, CI 설정이 없다.

## 디자인 시스템 제약 (`DESIGN.md`)

랜딩페이지는 CRT / 오락실 캐릭터-셀렉트 레트로 무드를 의도적으로 채택했다. `index.html`을 수정할 때 임의로 바꾸지 말고 아래 규칙을 지킬 것.

- **컬러 토큰** (`:root`의 CSS 커스텀 프로퍼티): `--bg`, `--panel`, `--green`, `--green-dim`, `--amber`, `--ink`. `--amber`는 "여기를 누르세요/선택하세요"라는 의미이므로 **페이지당 5곳 이하**로 제한해야 한다 — 라벨·배지·장문 텍스트에 앰버를 추가하지 말 것. 장문 본문은 반드시 `--ink`를 쓰고 `--green`은 쓰지 않는다 (형광 그린으로 긴 글을 읽으면 눈이 아프다). `--green-dim`은 테두리/구분선 전용이며 텍스트로 쓰지 않는다.
- **폰트**: Galmuri14(히어로 디스플레이)와 Galmuri11(제목/라벨/버튼/숫자)은 비트맵 도트폰트라 **설계 크기의 정수배**에서만 선명하다 (Galmuri11: 11/22/33/44px, Galmuri14: 14/28/42/56/70px). 도트폰트 요소에는 `clamp()`나 `vw`를 쓰지 말고, 반응형은 미디어쿼리로 크기 단계를 바꾼다. 본문은 Pretendard.
- **둥근 모서리 금지** (`border-radius: 0` 전역) — 픽셀 감성과 충돌한다.
- **테두리는 `2px solid`** (1px는 흐릿해서 레트로 느낌이 안 산다), **그림자는 블러 없는 하드 섀도우**: `box-shadow: 4px 4px 0 var(--green-dim)`.
- **애니메이션은 히어로에 집중**되어 있다 (셀렉트 그리드 자동 순회, 스캔라인, 버튼 눌림). S-2 이후 섹션은 의도적으로 정적 상태를 유지해 히어로가 유일한 "쇼"로 남게 한다.
- `prefers-reduced-motion: reduce`를 항상 존중할 것 (이미 적용되어 있으니, 애니메이션 요소를 추가할 때도 유지할 것).
- 섹션 간격은 8px 그리드를 따르고, 컨테이너 최대 너비는 1080px.

## 랜딩페이지 구조 (`PRD.md` §5, `DESIGN.md` §7)

`index.html`은 아래 순서로 고정된 7개 섹션으로 이루어진 하나의 스크롤 페이지다. 콘텐츠를 추가·수정할 때는 새 최상위 섹션을 만들지 말고 해당하는 섹션 안에 넣을 것.

| 섹션 | 목적 |
|---|---|
| S-1 Hero | 로고, 서브카피, 캐릭터 셀렉트 음식 그리드 (자동 순회, 클릭/호버 시 선택되며 CTA 문구가 바뀜), 메인 CTA |
| S-2 문제 제기 | 왜 모임이 결정을 못 내리는지 공감 카드 3개 |
| S-3 솔루션 흐름 | 3스텝 흐름 (STEP 01~03); 3번째 스텝(투표)은 아직 구현되지 않아 `예정` 배지 표기 |
| S-4 핵심 기능 | 기능 카드 3개; 투표 기능 카드도 마찬가지로 `예정` 배지 |
| S-5 사용 시나리오 | RPG 대사창 스타일로 구체적 상황 1개 |
| S-6 최종 CTA | 이메일 등록 폼 — 클라이언트 사이드로만 동작, 백엔드 없음. 제출 시 "등록 완료" 상태로 전환될 뿐 |
| S-7 Footer | 서비스명 + GitHub 링크 |

## 로드맵 맥락 (`PRD.md` §3)

Phase 1(이 랜딩페이지)만 구현되어 있다. 별도 요청이 없는 한 Phase 2(리뷰 리스트/검색, Gemini 기반 AI 요약, 선택적 Supabase 인증, 감성 대시보드)나 Phase 3(실시간 투표) 기능을 이 저장소에 구현하지 말 것 — 랜딩페이지는 아직 없는 기능을 숨기거나 꾸며내는 대신 `예정` 배지로 의도적으로 드러낸다.

## 반응형
- 모바일 (375)
- 태블릿 (768)
- 데스크탑 (1440)
으로 브레이크포인트 설정